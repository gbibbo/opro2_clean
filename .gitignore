# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv

.claude/settings.local.json

# Conda
*.conda
*.tar.bz2

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Claude Code
.claude/settings.local.json

# Jupyter (keep important notebooks)
.ipynb_checkpoints
# Exclude exploratory notebooks, keep documented ones
notebooks/*_scratch.ipynb
notebooks/*_temp.ipynb

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Data (keep structure, ignore content)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
data/segments/*
!data/segments/.gitkeep
!data/segments/**/.gitkeep
data/cache/*
!data/cache/.gitkeep

# ESC-50 dataset (cloned repo)
data/ESC-50/

# Models and checkpoints (downloaded model files only, not Python code)
*.pt
*.pth
*.bin
*.safetensors
checkpoints/
models/*.bin
models/*.safetensors
models/*.pt
models/*.pth
wandb/

# Allow Python model implementations
!src/qsm/models/
!src/qsm/models/*.py

# Logs (all test outputs)
logs/
*.log

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# MyPy
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff
.ruff_cache/

# Environment variables
.env
.env.local

# Large files - Audio/Video (DO NOT commit to GitHub)
*.wav
*.mp3
*.mp4
*.mkv
*.webm
*.avi
*.flac
*.m4a
*.ogg
*.opus

# Dataset-specific (auto-generated configs)
# Note: These are generated by scripts/download_datasets.py
configs/datasets/*.yaml
!configs/datasets/.gitkeep
!configs/datasets/README.md

# Downloaded datasets - videos and archives
data/raw/**/*.zip
data/raw/**/*.tar
data/raw/**/*.tar.gz
data/raw/**/*.tar.bz2
data/raw/**/videos/
data/raw/**/repo/

# Annotations (keep small text files, ignore large CSVs if needed)
data/raw/**/*.csv
!data/raw/**/*_sample.csv

# Experiment results (large files only)
results/*.png
results/*.jpg
results/*.jpeg
results/*.wav
results/*.mp3
outputs/
assets/paper_figs/*
!assets/paper_figs/.gitkeep

# Allow evaluation results (parquet files are small and essential)
!results/*.parquet
!results/*.csv
!results/*.json

# Coverage reports
.coverage.*
coverage.xml

# Sprint 5 additions - Psychoacoustic conditions
# RIR dataset (large: 60,000+ files, ~8GB from OpenSLR SLR28)
data/external/

# Evaluation results directory (most outputs, but keep critical files)
results/
# BUT keep baseline results
!results/sprint6_robust/*.parquet
!results/sprint6_robust/*.csv
!results/psychometric_curves/*.json
!results/psychometric_curves/*.png
!results/sprint8_factorial/*.parquet
!results/sprint8_factorial/*.json
!results/sprint8_stratified/*.json
!results/sprint8_stratified/*.png
!results/sprint8_glmm/*.json
!results/sprint8_glmm/*.png
!results/test_set_final/*.parquet
!results/test_set_final/duration_curves/*.json
# Keep prompt optimization results
!results/prompt_opt_local/*.png
# Keep final publication figures
!results/figures/
!results/figures/*.png

# Scratch/temporary documentation
docs/scratch/

# Archived documentation (historical, not critical)
docs/archive/

# Windows error files
nul

# Slurm output files (keep in logs/ which is already gitignored)
*.out
*.err

# Keep evaluation results for base and LoRA models
!results/eval_base_thresh*/*.csv
!results/eval_base_thresh*/*.json
!results/eval_lora_thresh*/*.csv
!results/eval_lora_thresh*/*.json

# NOTE: legacy/ folder is tracked and should NOT be ignored
# It contains archived code that may be useful for reference
